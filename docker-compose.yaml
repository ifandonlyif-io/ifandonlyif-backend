version: "3.9"
services:
  postgres:
    image: postgres:14-alpine
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=iffdev
    ports:
      - "5433:5432"
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:1323"
    environment:
      - DB_SOURCE=postgresql://root:secret@postgres:5432/iffdev?sslmode=disable
    depends_on:
      - postgres
    entrypoint:
      [
        "/app/wait-for.sh",
        "postgres:5432",
        "--",
        "/app/start.sh"
      ]
    command: [ "/app/main" ]

